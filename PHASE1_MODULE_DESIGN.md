# 第一阶段模块设计（Platform）

## 目标

第一阶段只做非系统管理部分的业务闭环骨架，重点把“资产、运行、告警、结算、运维、总览”串成可用链路。

## 范围

- 经营总览
- 设备档案
- 能源运营
- 告警处置
- 结算收款
- 设备运维
- 个人中心

系统管理只保留为依赖底座，不作为第一阶段主战场。

## 统一约定

- 所有业务都必须支持组织范围过滤
- 所有详情页都要能回跳来源列表
- 所有关键动作都要写操作审计
- 所有关键状态都要可追溯
- 所有跨页跳转都通过 query 参数传递上下文

## 模块一：经营总览

### 目标

把系统状态收口为一个运营驾驶舱。

### 第一阶段内容

- 当前组织切换
- 指标汇总
- 告警待办
- 账单待收
- 设备在线概况
- 能耗趋势

### 后端任务

- `dashboard/summary` 支持 `rootOrgId`
- `workspace/cockpit` 支持 `rootOrgId`
- 总览指标按组织子树过滤
- 总览卡片增加可钻取字段

### 验收

- 切换组织后，总览指标同步变化
- 每个卡片可定位到对应子页

## 模块二：设备档案

### 目标

把组织、网关、设备、测点、协议映射打通成资产中枢。

### 第一阶段内容

- 组织档案树
- 设备卡片档案
- 设备详情页
- 新增/复制/编辑/删除
- 删除链路约束

### 后端任务

- `org-tree`、`device-tree` 输出统一树结构
- `archive-profile` 返回基础信息、实时、历史、告警、指令摘要
- 组织/网关/设备删除前做关联校验
- 设备类型、测点定义、协议映射完整性检查

### 验收

- 组织有子节点不能删
- 网关有设备不能删
- 设备详情能直接看到运行和告警摘要

## 模块三：能源运营

### 目标

把实时、历史、质量做成一个数据闭环。

### 第一阶段内容

- 实时监控
- 历史曲线
- 能耗统计
- 数据质量

### 后端任务

- 实时数据按设备和测点统一返回
- 历史数据支持时间范围和多测点
- 统计数据支持日/月聚合
- 数据质量支持完整率与重算
- 异常数据要能回跳告警和设备

### 验收

- 设备切换后数据同步变化
- 历史、质量、实时能互相跳转

## 模块四：告警处置

### 目标

把告警做成事件闭环，而不是告警列表。

### 第一阶段内容

- 告警事件中心
- 告警处置台
- 告警规则

### 后端任务

- 告警查询按组织、设备、等级、状态过滤
- 告警处置支持备注、人员、时间
- 告警规则支持启用/停用
- 告警事件返回关联设备和规则信息

### 验收

- 告警事件能跳设备详情
- 处置结果能回写状态和审计

## 模块五：结算收款

### 目标

把用量、规则、账单、缴费形成经营闭环。

### 第一阶段内容

- 结算工作台
- 账单中心
- 计费账户
- 计费规则
- 规则适用范围
- 阶梯价格明细

### 后端任务

- 账单预览
- 账单生成
- 缴费
- 重算
- 作废
- 账单快照
- 账单查询支持组织范围和设备穿透

### 验收

- 账单能反查生成依据
- 账单可从设备历史数据追溯

## 模块六：设备运维

### 目标

把接入诊断、指令控制、执行结果串成运维闭环。

### 第一阶段内容

- 接入诊断
- 设备控制台
- 指令追踪

### 后端任务

- 网关在线状态查询
- 原始报文查看
- 指令下发
- 指令状态追踪
- 指令结果关联设备健康

### 验收

- 下发指令后可追踪执行状态
- 执行成功后可刷新设备实时数据

## 模块七：个人中心

### 目标

提供个人工作视图。

### 第一阶段内容

- 当前会话
- 组织范围
- 角色权限
- 我的操作记录

### 验收

- 用户能明确看到自己能管什么、最近做了什么

## 开发顺序

1. 设备档案
2. 能源运营
3. 告警处置
4. 结算收款
5. 设备运维
6. 经营总览联调
7. 个人中心

